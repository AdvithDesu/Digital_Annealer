\documentclass[9pt]{beamer}

\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{makecell}
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{svg}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{url}
\usepackage{physics}
\usepackage{amssymb}
\usepackage{float}
\usepackage{subcaption} % loads the caption package

\usepackage{pgfplots}
\pgfplotsset{compat=1.18}

\usepackage{algorithm}
\usepackage{algpseudocode}

\usepackage{wrapfig}
\usepackage{minted}
\usepackage{xcolor}
\usepackage{media9}
\usepackage{multimedia}  % For embedding videos
% \usepackage[backend=biber,style=numeric]{biblatex} % Adjust style as needed
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    filecolor=magenta,      
    urlcolor=blue,
}
% Theme and customization
\usetheme{default} % Change this to try different themes
\usecolortheme{beaver} % Change color scheme as needed
\setbeamertemplate{navigation symbols}{} % Remove navigation symbols

% Title and author information
\title{Integer Factorization as an Ising problem}
% \subtitle{Optional Subtitle}
\author{Advith Desu}
\institute{IIT Madras}
\date{\today}

\begin{document}

% Title slide
\begin{frame}
    \titlepage
\end{frame}

% % Table of contents
% \begin{frame}{Table of Contents}
%     \tableofcontents
% \end{frame}

\begin{frame}{GPU Kernel}

    Digital Annealer 1 (DA1) manually launches kernels by assigning specific threads to do the energy calculations. See \href{https://github.com/AdvithDesu/QAP-using-Simulated-Annealing-optimized-for-GPU}{\text{GitHub Link}} for the code used.

    \bigskip

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.85\textwidth]{GPU Threads.png}
        \caption{GPU Threads and Thread blocks.}
        \label{fig: GPU Threads and Thread blocks.}
    \end{figure}
    
\end{frame}

\begin{frame}{GPU Thread Calculation in DA1}

    Each thread $i$ calculates the local field $L_i = \sum_j J_{ij}s_j + h_i$ and the energy difference is then calculated as $-2 \times s_i \times L_i$ depending on which $i^{\text{th}}$ spin flipped.

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.65\textwidth]{Energy calculation in DA1.png}
        \caption{Energy calculation in DA1.}
        \label{fig: Energy calculation in DA1.}
    \end{figure}
    
\end{frame}

\begin{frame}{Pseudo-code for Digital Annealer 1 (DA1)}

    \begin{algorithm}[H]
\caption{Pseudo-code for Digital Annealer 1}
\begin{algorithmic}[1]
\Require Initial temperature, number of iterations $iters$, connectivity matrix $J[N][N]$ and magnetic field $h[N]$
\Ensure Spin configuration $s[N]$

\State \textbf{Initialization:} Randomly initialize spins $s[N]$, $k = 0$
\State Copy $J[N][N]$ and $h[N]$ to GPU memory

\While{$k \leq iters$}
    \For{$i = 1 : N$} \Comment{$N$ blocks for $N$-spin model}
        \For{$j = 1 : N/P$}
            \State Calculate partial sum of $\Delta H(s_i)$ by computing $J[i][j] \cdot s[j]$
        \EndFor

        \For{$p = 1 : \log(P)$} \Comment{Using $P$ threads}
            \State Reduction of partial sum of $\Delta H(s_i)$
        \EndFor

        \State Calculate $P_B(\Delta H, T)$ for $\Delta H(s_i)$ to determine state of spin $s_i$
    \EndFor \Comment{For all $N$ spins in ensemble}

    \State Update all possible spins
\EndWhile
\State Decrease temperature $T$ linearly

\end{algorithmic}
\end{algorithm}

    
\end{frame}


\begin{frame}[allowframebreaks]{Pseudo-code for DA1 with fractional flips}


\begin{algorithmic}[1]

\Require Initial temperature, number of iterations $iters$, connectivity matrix $J[N][N]$, magnetic field $h[N]$, flip fraction $\rho$
\Ensure Spin configuration $s[N]$

\State \textbf{Initialization:} Randomly initialize spins $s[N]$
\State Copy $J$, $h$, and $s$ to GPU memory

\While{$k \leq iters$}

    \State $A \gets \emptyset$ \Comment{List of Metropolis-accepted spins}

    % ----------- Phase 1: Compute candidates -----------
    \For{$i = 1 : N$ \textbf{in parallel}} \Comment{$N$ GPU blocks}

        \For{$j = 1 : N/P$} \Comment{$P$ threads per block}
            \State Compute partial sum of local field contribution:
            \[
                S_i \mathrel{+}= J[i][j] \cdot s[j]
            \]
        \EndFor

        \For{$p = 1 : \log(P)$} \Comment{Parallel reduction}
            \State Reduce partial sums to obtain full local field $S_i$
        \EndFor
        \State Compute energy change:
        \[
            \Delta H_i = -2\, s[i]\,(S_i + h[i])
        \]

        \State Compute Metropolis probability:
        \[
            P_B = \min\{1,\exp(-\Delta H_i/T)\}
        \]

        \If{random() $< P_B$}
            \State Append index $i$ to $A$
        \EndIf

    \EndFor
    \Comment{None of the spins are flipped}

    % ----------- Phase 2: Exact-k subset selection -----------
    \State Let $M \gets |A|$ \Comment{Total number accepted}

    \State $k \gets \lceil \rho \cdot M \rceil$ \Comment{Exact number of flips to perform}

    \State Randomly shuffle the list $A$ (Fisher--Yates)
    \State $A_k \gets$ first $k$ elements of $A$ \Comment{Random subset of size $k$}

    % ----------- Phase 3: Apply selected flips -----------
    \For{each $i \in A_k$ \textbf{in parallel}}
        \State Flip spin: $s[i] \gets -s[i]$
    \EndFor

    \State Update iteration counter: $k \gets k + 1$

\EndWhile

\State Decrease temperature $T$ linearly
\State \Return $s[N]$

\end{algorithmic}

\end{frame}

%====================================================
\section{Introduction}
%====================================================

\begin{frame}{Factorization Problem}
\begin{itemize}
\item Given a natural number $N$, find $P,Q$ such that
\[
N = P Q
\]
\item Hard inverse problem underlying RSA security.
\item Idea: map factorization $\rightarrow$ optimization problem.
\item Solve using annealing-based techniques.
\end{itemize}
\end{frame}

%====================================================
\section{Formulation}
%====================================================

\begin{frame}{Binary Representation and Assumptions}
Assumptions:
\begin{itemize}
\item $N$ is odd.
\item Factors have equal bit-length
\[
n=\left\lceil \tfrac12 \log_2 N \right\rceil.
\]
\end{itemize}

Binary form:
\[
P=\sum_{k=0}^{n-1}2^k p_k,\quad
Q=\sum_{k=0}^{n-1}2^k q_k,\quad p_k,q_k\in\{0,1\}.
\]

Fixed bits:
\[
p_0=q_0=1,\qquad p_{n-1}=q_{n-1}=1.
\]
\end{frame}

\begin{frame}{Column-wise Multiplication}
\begin{itemize}
\item Construct binary long multiplication table.
\item Introduce carry variables $s_{i,j}$.
\item Each column gives a constraint:
\[
C_i = N_i - \sum_{j} q_j p_{i-j}
      - \sum s_{j,i}
      + \sum 2^j s_{i,i+j}.
\]
\item Correct factors satisfy $C_i = 0$.
\end{itemize}
\end{frame}

\begin{frame}{Energy Function}
Optimization objective:
\[
\min_{\{p_i,q_i,s_{ij}\}} \sum_i (C_i)^2.
\]

\begin{itemize}
\item Binary variables: factor bits + carry bits.
\item Ground state corresponds to valid factorization.
\item Alternative direct method:
\[
\left(N-\sum p_i2^i \sum q_j2^j\right)^2
\]
but gives fewer preprocessing opportunities.
\end{itemize}
\end{frame}

%====================================================
\section{Classical Pre-processing}
%====================================================

\begin{frame}{Core Reduction Rules}
For binary variables $x_i\in\{0,1\}$:

\textbf{Sum rules}
\[
\sum_i x_i=n \Rightarrow x_i=1,\qquad
\sum_i x_i=0 \Rightarrow x_i=0.
\]

\textbf{Complement rule}
\[
x_1+x_2=1 \Rightarrow x_2=1-x_1.
\]

\textbf{Power rule}
\[
x^m=x,\quad m\ge2.
\]
\end{frame}

\begin{frame}{Coefficient and Parity Rules}
General linear constraint:
\[
\sum_i c_i x_i + C = 0.
\]

Let
\[
S_+=\sum_{c_i>0}c_i,\quad S_-=\sum_{c_j<0}|c_j|.
\]

\begin{itemize}
\item Large coefficients can force $x_i=0$ or $1$.
\item Parity constraints:
\begin{align*}
x_1+x_2+\text{even} &= \text{even} \Rightarrow x_1=x_2,\\
x_1+x_2+\text{even} &= \text{odd} \Rightarrow x_1+x_2=1.
\end{align*}
\end{itemize}
\end{frame}

\begin{frame}{Example: $N=15$ Reduction}
Initial clauses (example):
\begin{align*}
C_1 &: -p_1-q_1+2s_{1,2}+1=0,\\
C_2 &: -p_1q_1-p_2-q_2-s_{1,2}+2s_{2,3}+4s_{2,4}+1=0.
\end{align*}

Applying rules:
\begin{itemize}
\item Fix carries $s_{1,2}=s_{2,4}=0$.
\item Complement relation: $q_1=1-p_1$.
\item Power rule: $p_1^2=p_1$.
\end{itemize}

Final reduced constraint:
\[
2p_1p_2-p_1-p_2+1=0
\Rightarrow p_1+p_2=1.
\]
\end{frame}


\begin{frame}[allowframebreaks]{J matrix Visualization}

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.62\textwidth]{Sparsity of J matrix for 162 BQM.png}
        \caption{Sparsity of J matrix for 162 BQM  (Integer 39203).}
        \label{fig: Sparsity of J matrix for 162 BQM (Integer 39203).}
    \end{figure}
    
\end{frame}

\begin{frame}{Sparse Coupling Matrix Representation (CSR)}

\textbf{Problem:}
The Ising coupling matrix $J_{ij}$ is very large but mostly zero.
Storing it as a dense $N \times N$ matrix wastes memory and bandwidth.

\vspace{0.3cm}
\textbf{Key idea:}
Store \emph{only the nonzero couplings} using
\alert{Compressed Sparse Row (CSR)} format. CSR represents a sparse matrix using three arrays:

\[
\texttt{row\_ptr}[i] \quad \text{start index of row } i
\]
\[
\texttt{col\_idx}[k] \quad \text{column index of nonzero entry}
\]
\[
\texttt{values}[k] \quad \text{value of } J_{ij}
\]

Only nonzero entries are stored.

\vspace{0.2cm}
\textbf{Memory cost:} $\mathcal{O}(N + \text{nnz})$ instead of $\mathcal{O}(N^2)$

\end{frame}


\begin{frame}{Time Taken for J construction and CSR DA1}

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.9\textwidth]{bqm variables vs time taken for J construction and DA1.png}
        \caption{BQM variables vs time taken for J construction and DA1.}
        \label{fig: bqm variables vs time taken for J construction and DA1.}
    \end{figure}
    
\end{frame}

%========================================

\begin{frame}{Number of Densely connected spins with scale}

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.9\textwidth]{bqm variables vs number of densely connected spins.png}
        \caption{BQM variables vs number of densely connected spins.}
        \label{fig: bqm variables vs number of densely connected spins.}
    \end{figure}
    
\end{frame}

\begin{frame}{Number of Non-zero values in J matrix with scale}

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.9\textwidth]{bqm variables vs number of non zero values in J.png}
        \caption{BQM variables vs number of non zero values in $J_{ij}$.}
        \label{fig: bqm variables vs number of non zero values in J.}
    \end{figure}
    
\end{frame}

\begin{frame}{Energy and Average Energy difference with scale}

    \begin{figure}[htp]
        \centering
        \includegraphics[width=0.9\textwidth]{bqm variables vs absolute energy value and average delta E vales.png}
        \caption{BQM variables vs absolute energy value and average $\delta E$ values.}
        \label{fig: bqm variables vs absolute energy value and average delta E values.}
    \end{figure}
    
\end{frame}

\begin{frame}{Trend-lines}

\[
\text{Number of BQM variables (bqm)} \leq ( \left\lceil log_2{N} \right\rceil )^3
\]

\[
\text{Number of non-zero } J_{ij} \text{ elements (nnz)} = 6.08 \times \text{bqm} + 680
\]

\[
\text{time} (s) = 4.22 \times10^{-3} \times \text{bqm}
\]

\bigskip

\begin{itemize}

\item For a 128 bit number (N), it would have approximately 2 million bqm variables, 194 MB to store $J_{ij}$ and would take 2.5 hrs to run on DA1.

\item For a 256 bit number (N), it would have approximately 17 million bqm variables, 1.5 GB to store $J_{ij}$ and would take 19.66 hrs to run on DA1

\end{itemize}
\end{frame}

\begin{frame}{Annealing Time per Iteration vs Number of Spins}

    \begin{figure}
        \centering
        \includegraphics[width=0.9\linewidth]{spins_vs_time_per_iteration_for_mac_gh200.png}
    \end{figure}

\end{frame}

\begin{frame}{Construction time for CSR J}

    \begin{figure}
        \centering
        \includegraphics[width=\linewidth]{csr_construction_python_vs_cpp.png}
    \end{figure}

\end{frame}

\begin{frame}{Execution Time Comparison}

    \begin{figure}
        \centering
        \includegraphics[width=\linewidth]{Execution Time for graph coloring SA.png}
    \end{figure}

\end{frame}

\begin{frame}{Solution Quality from Graph-Colored Simulated Annealing}

    \begin{figure}
        \centering
        \includegraphics[width=1\linewidth]{percentage_difference_predicted_factors.png}
    \end{figure}

\end{frame}

\begin{frame}{Solution Quality Measured by Gap between Real and Estimated P}

    \begin{figure}
        \centering
        \includegraphics[width=1\linewidth]{Percentage Gap between real and estimated P.png}
    \end{figure}

    \begin{enumerate}
        \item Average (\%) difference with square root N as the estimate = \textbf{10.33\%}
        \item Average (\%) difference with $P_{\text{pred}}$ from Simulated Annealing = \textbf{4.23\%}
        \item Average (\%) difference with mean of $P_{\text{pred}}$ and $N / Q_{\text{pred}}$ = \textbf{2.98\%}
    \end{enumerate}

\end{frame}

\begin{frame}{Time Taken to Find Exact primes by Brute Force}

    \begin{figure}
        \centering
        \includegraphics[width=1\linewidth]{Raw gap vs Time taken to find solution.png}
    \end{figure}

    $$\text{Time Taken (s)} = \frac{3.32 \times 10^{-9}}{\text{Threads}} \times \text{Gap (\%)} \times 2^n$$

\end{frame}



% \begin{frame}[allowframebreaks]{Normalized Energy value statistics}

% \begin{table}[h]
% \centering
% \begin{tabular}{l c}
% \hline
% Statistic & Normalized Energy \\
% \hline
% Best (max)   & 0.99993952 \\
% Worst (min) & 0.83004852 \\
% Mean        & 0.94595608 \\
% Median      & 0.96233060 \\
% Std. dev.   & 0.0425 \\
% \hline
% \end{tabular}
% \caption{Summary statistics of normalized energy values over 100 runs (for the 100 $max\_iter$ case).}
% \end{table}

% \begin{table}[h]
% \centering
% \begin{tabular}{l c}
% \hline
% Statistic & Normalized Energy \\
% \hline
% Best (max)   & 1. \\
% Worst (min) & 0.89970262 \\
% Mean        & 0.97642265 \\
% Median      & 0.98642584 \\
% Std. dev.   & 0.0284 \\
% \hline
% \end{tabular}
% \caption{Summary statistics of normalized energy values over 100 runs (for the 1000 $max\_iter$ case).}
% \end{table}

% \begin{table}[h]
% \centering
% \begin{tabular}{l c}
% \hline
% Statistic & Normalized Energy \\
% \hline
% Best (max)   & 0.99999606 \\
% Worst (min) & 0.91698491 \\
% Mean        & 0.98125215 \\
% Median      & 0.99774576 \\
% Std. dev.   & 0.0259 \\
% \hline
% \end{tabular}
% \caption{Summary statistics of normalized energy values over 20 runs (for the 10,000 $max\_iter$ case).}
% \end{table}
    
% \end{frame}

% \begin{frame}{Number of Bits vs Number of BQM variables}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=\textwidth]{bits vs number of bqm variables.jpeg}
%         \caption{Number of bits vs number of bqm variables. Excuse the lack of clarity I just had the image.}
%         \label{fig: bits vs number of bqm variables.}
%     \end{figure}
    
% \end{frame}

% \begin{frame}[allowframebreaks]{DA1 performance on QAP}

% \begin{itemize}
%     \item One \textbf{Iteration} is the process of calculating the energy difference (and subsequently the Metropolis-Hasting condition) for all spins in the ensemble at a fixed Temperature (T). Followed by all required flips.
%     \item \textbf{$\Delta$ T}  is the value of the Temperature step used for cooling. Updates after running `$max\_iter$' number of iterations (or when the early stopping condition is reached.
%     \item \textbf{max\_iter} is a predefined variable that sets the maximum number of iterations the algorithm performs at a given T.
%     \item We also stop iterating if the total energy difference after between two iterations is less than \textbf{0.01} and if this happens continuously for \textbf{5\%} of $max\_iter$ number of iterations.
%     \item We use initial temperature as \textbf{100.0} and final temperature as \textbf{0.01}.
%     \item Note that in the graph below, all other accepted solutions lie between 88\% and 90\% of optimal energy values
% \end{itemize}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.9\textwidth]{Accepted solutions versus delta T.png}
%         \caption{Number of Accepted solutions versus delta T. The values in the brackets represent number of solutions that reach 95\% of the optimal solution out of 100 independent runs.}
%         \label{fig: Accepted solutions versus delta T.}
%     \end{figure}

    
% \end{frame}

% \begin{frame}{DA1 time performance on QAP}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.9\textwidth]{Time taken for a single run versus delta T.png}
%         \caption{Time taken for a single run versus delta T.}
%         \label{fig: Time taken for a single run versus delta T.}
%     \end{figure}
    
% \end{frame}

% \begin{frame}{DA1 Time per Iteration}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.9\textwidth]{Average time per iteration in DA1.png}
%         \caption{Average time per iteration (ms) in DA1.}
%         \label{fig: Average time per iteration in DA1.}
%     \end{figure}
    
% \end{frame}


% \begin{frame}[allowframebreaks]{Energy difference distribution}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.9\textwidth]{Distribution of Energy difference values across iterations_first half.png}
%         \caption{Distribution of Energy difference values across iterations (1).}
%         \label{fig: Distribution of Energy difference values across iterations (1).}
%     \end{figure}
%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.9\textwidth]{Distribution of Energy difference values across iterations_second half.png}
%         \caption{Distribution of Energy difference values across iterations (2).}
%         \label{fig: Distribution of Energy difference values across iterations (2).}
%     \end{figure}
% \end{frame}

% \begin{frame}[allowframebreaks]{Time taken versus number of parallel runs}

% The graph shows the time taken by the GPU to complete a fixed number of iterations of the simulated annealing (the actual values of time is not important only the trend).

% \begin{figure}[htp]
%     \centering
%     \includegraphics[width=0.65\textwidth]{Time taken vs number of parallel runs.png}
%     \caption{Time taken vs Number of parallel runs. (problem scale 12)}
%     \label{fig: Time taken vs number of parallel runs.}
% \end{figure}

% \begin{itemize}
%     \item As its visible $2^{15}$ parallel runs (around 30k) for QAP of problem scale 12 is the most optimal for the GH200. This paired with an appropriate temperature schedule is able to find 95\% of the optimal solution in 0.44 seconds.
%     \item Its worth noting how the time taken increases at the start from $2^1$ to $2^9$ parallel runs even though we should have plenty of threads. This is likely due to the overhead cost (time taken to launch kernels, CPU to GPU memory transfer, etc), but I am not entirely sure as to why it scales the way it does and stays constant from $2^{10}$ to $2^{15}$. But the `doubling' in time that we see after $2^{15}$ is because the GPU is running in patches of $2^{15}$ runs as we hit the limit of available threads.
% \end{itemize}
    
% \end{frame}

% \section{GH200 Benchmarking of QAP}

% \begin{frame}{GH200 Benchmarking of QAP}

%    \begin{table}[h]
%    \scriptsize
%     \centering
%     \begin{tabular}{|c|c|c|c|c|}
%         \hline
%         \textbf{Problem Scale} & \textbf{GPU Energy} & \textbf{Gurobi 12.0 Energy} & \textbf{GPU Time (s)} & \textbf{Gurobi Time (s)} \\ \hline
%         3  & 2.34 & 2.34 & 0.0052 & 0.0314 \\ \hline
%         4  & 2.21 & 2.21 & 0.0061 & 0.0383 \\ \hline
%         5  & 5.72 & 5.72 & 0.0241 & 0.1082 \\ \hline
%         6  & 6.76 & 6.76 & 0.112  & 0.1748 \\ \hline
%         7  & 9.35 & 9.28 & 0.1428 & 0.4429 \\ \hline
%         8  & 9.83 & 9.83 & 1.78   & 0.3253 \\ \hline
%         9  & 16.29 & 16.26 & 24.67 & 0.5087 \\ \hline
%         10 & 20.33 & 20.27 & 33.41 & 5.9119 \\ \hline
%         11 & 22.91 & 22.72 & 45.59 & 77.9059 \\ \hline
%         12 & 29.83 & 29.23 & 62.05 & 847.4936 \\ \hline
%     \end{tabular}
%     \caption{Comparison of GPU and Gurobi Energy and Time}
%     \label{tab:energy_time_comparison}
% \end{table}
    
% \end{frame}

% \begin{frame}{Original Benchmark from the paper}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=\textwidth]{QAP Original Benchmark.png}
%         \bigskip
%         \caption{QAP Original Benchmark.}
%         \label{fig: QAP Original Benchmark.}
%     \end{figure}
    
% \end{frame}

% \begin{frame}{GH200 Plot}

%     \begin{figure}[htp]
%         \centering
%         \includegraphics[width=0.85\textwidth]{QAP Benchmark GH200.png}
%         \bigskip
%         \caption{QAP Benchmark GH200.}
%         \label{fig: QAP Benchmark GH200.}
%     \end{figure}
    
% \end{frame}

% \begin{frame}{GH200 Benchmark run wise values}

% \begin{table}[h]
%     \tiny
%     \centering
%     \begin{tabular}{ccccccccccc}
%         \toprule
%         Problem Scale & Run 1 & Run 2 & Run 3 & Run 4 & Run 5 & Run 6 & Run 7 & Run 8 & Run 9 & Run 10 \\
%         \midrule
%         3 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 & 2.34 \\
%         4 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 & 2.21 \\
%         5 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 & 5.72 \\
%         6 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 & 6.78 \\
%         7 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 & 9.83 \\
%         8 & 16.29 & 16.31 & 16.29 & 16.29 & 16.26 & 16.29 & 16.29 & 16.31 & 16.31 & 16.31 \\
%         9 & 20.46 & 20.46 & 20.49 & 20.49 & 20.56 & 20.46 & 20.46 & 20.46 & 20.49 & 20.49 \\
%         10 & 25.33 & 25.33 & 25.39 & 25.39 & 25.39 & 25.33 & 25.33 & 25.33 & 25.39 & 25.39 \\
%         11 & 22.92 & 22.67 & 20.94 & 20.82 & 23.96 & 22.92 & 22.92 & 22.92 & 22.80 & 22.67 \\
%         12 & 30.2 & 30.14 & 30.23 & 30.23 & 29.86 & 29.75 & 29.75 & 29.49 & 29.69 & 29.66 \\
%         \bottomrule
%     \end{tabular}
%     \caption{Performance by Run and Problem Scale}
%     \label{tab:performance_table}
% \end{table}
    
% \end{frame}

% \begin{frame}{GH200 Benchmarking parameters}

% % Requires: \usepackage{booktabs}
% \begin{table}[h]
%     \scriptsize
%     \centering
%     \begin{tabular}{cccc}
%         \toprule
%         Problem Scale & Parallel Sweeps & Number of Iterations & Temperature Parameters \\
%         \midrule
%         3  & 1000  & 4    & 1, 0.001, 0.97 \\
%         4  & 1000  & 5    & 1, 0.001, 0.97 \\
%         5  & 2000  & 20   & 1, 0.001, 0.97 \\
%         6  & 5000  & 100  & 1, 0.001, 0.97 \\
%         7  & 6000  & 1000 & 1, 0.001, 0.97 \\
%         8  & 10000 & 5000 & 1, 0.001, 0.97 \\
%         9  & 30000 & 8000 & 2, 0.001, 0.97 \\
%         10 & 30000 & 8000 & 2, 0.001, 0.97 \\
%         11 & 30000 & 8000 & 2, 0.001, 0.97 \\
%         12 & 30000 & 8000 & 2, 0.001, 0.97 \\
%         \bottomrule
%     \end{tabular}
%     \caption{Problem Scale, Parallel Sweeps, Number of Iterations, and Temperature Parameters.}
%     \label{tab:parameters}
% \end{table}
    
% \end{frame}

\end{document}
